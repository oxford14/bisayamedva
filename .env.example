NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
PAYMONGO_SECRET_KEY=
PAYMONGO_PUBLIC_KEY=
PAYMONGO_WEBHOOK_SECRET=
RESEND_API_KEY=
RESEND_FROM_EMAIL=Bisaya MedVA <info@bisayamedva.com>
NEXT_PUBLIC_APP_URL=https://bisayamedva.com

# Practice Lab — Mock Call (generate bundled caller MP3s once; students play from /public)
ELEVENLABS_API_KEY=
# Optional — live API fallback only; professional/library voices may require a paid plan
ELEVENLABS_VOICE_ID=
# Bundled Mock Call caller MP3s (use a premade voice_id from: npx tsx scripts/list-elevenlabs-voices.ts)
ELEVENLABS_CALLER_VOICE_ID=EXAVITQu4vr4xnSDxMaL
ELEVENLABS_MODEL_ID=eleven_multilingual_v2
# After changing scenario scripts:
#   npm run generate:mock-call-audio
# Regenerate all MP3s (overwrite existing):
#   npm run generate:mock-call-audio:force
# Or: FORCE_MOCK_CALL_AUDIO=1 npm run generate:mock-call-audio

# Supabase Dashboard → Authentication → URL configuration:
# Site URL = NEXT_PUBLIC_APP_URL
# Redirect URLs: {NEXT_PUBLIC_APP_URL}/auth/confirm and .../auth/reset-password
# (add www variant if you use it). Password reset emails are sent via Resend, not Supabase SMTP.
