NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
# 32 random bytes, base64 — encrypts registration checkout passwords until payment (openssl rand -base64 32)
REGISTRATION_INTENT_ENCRYPTION_KEY=
PAYMONGO_SECRET_KEY=
PAYMONGO_PUBLIC_KEY=
PAYMONGO_WEBHOOK_SECRET=
RESEND_API_KEY=
RESEND_FROM_EMAIL=Bisaya MedVA <info@bisayamedva.com>
NEXT_PUBLIC_APP_URL=https://bisayamedva.com

# Supabase Dashboard → Authentication → URL configuration:
# Site URL = NEXT_PUBLIC_APP_URL
# Redirect URLs: {NEXT_PUBLIC_APP_URL}/auth/confirm and .../auth/reset-password
# (add www variant if you use it). Password reset emails are sent via Resend, not Supabase SMTP.
